# Variables de entorno para NoteFlow

# JWT Secret para autenticación
JWT_SECRET=tu_super_secreto_jwt_aqui

# API Key para OpenRouter (IA)
OPENROUTER_API_KEY=tu_api_key_de_openrouter_aqui

# Puerto del servidor (opcional, Vercel lo maneja automáticamente)
PORT=3001

# Entorno
NODE_ENV=production 